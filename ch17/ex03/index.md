## 問題

- npm に同梱されている npx を利用することにはどのような利点があるのか説明しなさい。

## 解答

- npxはパッケージをグローバル環境にインストールせずに実行できるため以下のような利点がある
  - 使用頻度が低いものはnpmで管理せずにnpxで実行すれば管理するパッケージを増やさずに済む
    - 例えば`create-react-app`のようなプロジェクトの開始時にしか使わないパッケージなど
  - バージョン指定で試したいときに現在の環境に影響を与えずに試せる
  - 実行時に最新版をチェックするため常にパッケージの最新版を使用できる
